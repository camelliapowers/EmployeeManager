
@*@{
    ViewData["Title"] = "List";
}

<h1>List</h1>*@

@model List<Employee> 
<h2>List of Employee</h2>
<h2 class="message">@TempData["Message"]</h2>

<a asp-controller="EmployeeManager" asp-action="Insert" class="btn btn-primary">Insert</a>

<table border="1" style="margin:10% auto; width:900px;">
    <tr>
        <th>Employee ID </th>
        <th>Frist Name</th>
        <th>Last Name</th>
        <th>Title</th>
        <th colspan="2">Actions</th>
    </tr>
    @foreach (var item in Model) 
    { 
    <tr>
        <td>@item.EmployeeID</td>
        <td>@item.FirstName</td>
        <td>@item.LastName</td>
        <td>@item.Title</td>
        <td>
            <a asp-controller="EmployeeManager" asp-action="Update" asp-route-id="@item.EmployeeID" class="btn-link">Update</a>
        </td>
        <td>
            <a asp-controller="EmployeeManager" asp-action="Delete" asp-route-id="@item.EmployeeID" class="btn-link">Delete</a>
        </td>
    </tr>
    }
</table>